<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<!-- 目的:为 dao接口方法提供 sql语句 -->
<mapper namespace="moe.harusame.dao.FriendDao">

	<insert id="sendInvitation">
		insert ignore into friend (friend_id, user_id, state_friend, state_user)
		values (#{friendId}, #{userId}, 1, 0)
	</insert>

	<select id="inviteMe" resultType="Friend">
		select friend_id, user_id, state_friend, state_user
		from friend
		where friend_id = #{userId}
		and state_friend = 1
		and state_user = 0
	</select>
	
	<select id="checkMyInvitation" resultType="Friend">
		select friend_id, user_id, state_friend, state_user
		from friend
		where user_id = #{userId}
		and state_friend = 1
		and state_user = 0
	</select>
	
	<select id="getFriendsId" resultType="Friend">
		select friend_id, user_id
		from friend
		where user_id = #{userId}
		and state_friend = 0
		and state_user = 0
		or friend_id = #{userId}
	</select>
	
	<update id="acceptInvitation">
		update 
			friend
		set
			state_friend = 0
		where friend_id = #{userId} 
		and user_id = #{friendId}
		and state_friend = 1
		and state_user = 0
	</update>
	
</mapper>
